function printArray(Javascript) {
    for (let inc = 0; inc < Javascript.length; inc++) {
      console.log(Javascript[inc]);
    }
  }
  
  const arr = ['you asked us to read on the following', 'array', 'functions', 'loop', 'control-flows', ];
  printArray(arr);